apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: simple-time-service-ingress
  annotations:
    alb.ingress.kubernetes.io/group.name: trainerkarthik-shop-staging
    cert-manager.io/cluster-issuer: letsencrypt-staging
spec:
  rules:
    - host: staging.time.kart24.shop
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: simple-time-service
                port:
                  number: 80
  tls:
    - hosts:
        - staging.time.kart24.shop
      secretName: staging-time-tls

